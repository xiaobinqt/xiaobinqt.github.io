name: xiaobinqt.github.io

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build ...
        with:
          node-version: 10.x
          script: |
            npm -v
            npm install gitbook-cli -g
            

