name: xiaobinqt.github.io

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build ...
        with:
          node-version: 10.x
      run: |
        echo 111111111
        npm -v
        npm install gitbook-cli -g
            

